extends ../includes/layout

block main-content
  header#header(wpm-header data-current-skin="blue")
  section#main
    aside#sidebar(wpm-sidebar ng-class="{ 'toggled': sidebarCtrl.sidebarStatus.left === true }")
    section#content(ui-view="content")
  footer#footer(wpm-footer)